import React from 'react'
import List from '../List/List'

export default function Today (props)
{
  return (
    <div className="today">
      {/* {
        lists.map(list => <List />)
      } */}
    </div>
  )
}
